# Copy this file to .env and fill in your real values.
# NEVER commit the .env file — it is listed in .gitignore.

# RPC endpoint for the Sepolia testnet.
# Get one free from Infura (https://infura.io) or Alchemy (https://alchemy.com).
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_PROJECT_ID

# Private key of the deployer wallet (WITHOUT the 0x prefix).
# Use a dedicated test wallet — never use a wallet that holds real funds.
PRIVATE_KEY=your_wallet_private_key_here
